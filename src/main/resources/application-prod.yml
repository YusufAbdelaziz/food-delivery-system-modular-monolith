# Keys like URL, DB name, and DB credentials should be provided by the environment.
# They are hardcoded here for practice purposes.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food-delivery-prod?createDatabaseIfNotExist=true
    username: joe
    password: "vv44sRUF#NCtu7zKcfR5uYdps6*"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  flyway:
    clean-disabled: true

logging:
  level:
    root: INFO
    org:
      springframework:
        web: INFO
      hibernate: ERROR
    com:
      joe:
        abelaziz: INFO
  file:
    path: /logging/global/logs
    name: ${logging.file.path}/global.log

application:
  security:
    jwt:
      secret-key: f553403da766f00ec6ecebc4c4a547789a1a7aafd083403e2ff7f8a8fb168c4f
      expiration: 86400000
      refresh-token:
        expiration: 604800000

scheduler:
  update-restaurant-ratings:
    rate: 86400000
  update-courier-ratings:
    rate: 3600000
